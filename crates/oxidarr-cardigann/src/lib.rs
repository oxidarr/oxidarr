//! Cardigann v11 indexer definition engine
//!
//! Parses and executes Prowlarr-compatible Cardigann YAML definitions (schema v11).
//! Covers the 21 filter functions, the 7-directive template subset, the 4 login
//! flows, and jQuery-to-CSS selector rewriting. Network client is injected as a
//! trait so the engine is testable against recorded fixtures.

/// Placeholder export. Scaffolding only — no business logic yet.
pub const CRATE_NAME: &str = "oxidarr-cardigann";
